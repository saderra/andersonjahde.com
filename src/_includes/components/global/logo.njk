<div>
    {% if layout == 'home' %}
    <a href="/" class="inline-block bg-white rounded-xl shadow-lg p-6 md:p-8">
        <img src="/assets/images/logo.png" alt="{{ site.title }} — {{ site.author.credentials }}" width="437" height="151" class="w-auto h-20">
    </a>
    {% else %}
    <a href="/">
        <img src="/assets/images/logo.png" alt="{{ site.title }} — {{ site.author.credentials }}" width="437" height="151" class="w-auto h-24">
    </a>
    {% endif %}
</div>
